<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 내용 배경 (80% 투명한 검정색) -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/balloon_bg"/>
            <corners android:radius="8dp" />
            <padding
                android:left="12dp"
                android:top="12dp"
                android:right="12dp"
                android:bottom="20dp" />
        </shape>
    </item>

</layer-list>
